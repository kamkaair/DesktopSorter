# DesktopSorter
DesktopSorter is a personal automatization tool and practice project.
This software has support for managing and sorting batches of files for targeted groups of files, which can be modified. Software supports filtering by file type and a file tagging system (based on the filename), which creates individual collections and prioritizes over file type filtering.
